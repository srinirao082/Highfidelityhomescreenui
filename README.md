
  # High-Fidelity Home Screen UI

  This is a code bundle for High-Fidelity Home Screen UI. The original project is available at https://www.figma.com/design/KRs0UrnV1BCaNdS9SGC6zp/High-Fidelity-Home-Screen-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  